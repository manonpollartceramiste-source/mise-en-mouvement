"use server";

// La création client est gérée par /api/os/clients/create (route POST stable).
// Ce fichier est conservé pour éviter des erreurs d'import si des chunks anciens
// circulent encore en CDN cache — il n'exporte plus d'action active.
